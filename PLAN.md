# PLAN — BMI計算機

## 1. 概要

身長（cm）と体重（kg）を入力すると、BMI 値（体重kg ÷ (身長m)²）を小数点第1位まで自動計算して表示し、日本肥満学会の基準に基づく肥満度判定（低体重・普通体重・肥満（1度）〜肥満（4度））を合わせて表示する静的単一ページアプリ。入力のたびにリアルタイムで再計算され、ボタン操作は不要。バニラJS・`public/index.html` 単一ファイルで完結し、Cloudflare Workers assets で配信する。

## 2. 受け入れ条件

- [ ] 身長 170、体重 60 を入力すると、BMI 値「20.8」が小数点第1位までの表記で表示される
- [ ] BMI 値に応じた判定ラベルが表示される。区分は日本肥満学会基準に従う: 18.5 未満「低体重」、18.5 以上 25 未満「普通体重」、25 以上 30 未満「肥満（1度）」、30 以上 35 未満「肥満（2度）」、35 以上 40 未満「肥満（3度）」、40 以上「肥満（4度）」（例: 170cm/50kg → 17.3「低体重」、170cm/75kg → 26.0「肥満（1度）」）
- [ ] 身長または体重の入力値を変更するたびに、ボタン操作なしで BMI 値と判定ラベルが再計算されて更新される（例: 170cm/60kg → 20.8 の状態から体重を 80 に変えると 27.7「肥満（1度）」に変わる）
- [ ] 身長・体重のいずれかが未入力または 0 以下の場合、BMI 値と判定ラベルは表示されず、入力を促すプレースホルダ表示（例: 「身長と体重を入力してください」）になる
- [ ] ページ最下部に `https://apps.jozo.beer` へのフッターリンク（テキスト「apps.jozo.beer」）が表示される

## 3. 実装方針

`public/index.html` 単一ファイル（CSS/JS インライン、ビルドなし、外部リソースなし）で実装する。

### 技術・レイアウト

- **マークアップ**: `<main>` コンテナ内に、タイトル、身長入力（`<input type="number" id="height">`、単位 cm）、体重入力（`<input type="number" id="weight">`、単位 kg）、結果表示領域（BMI 値 + 判定ラベル）を縦に並べる。`inputmode="decimal"` を付与しモバイル入力に配慮する
- **スタイル**: インライン `<style>`。ヘルスケア系の落ち着いた配色でカード型レイアウトを中央寄せ。センタリングは body を `display:flex; flex-direction:column; align-items:center` とし、AGENTS.md の制約どおり footer が flex アイテムになっても縦フローの最下部に来るようにする
- **favicon**: `<link rel="icon" href="data:image/svg+xml,...">` のインライン data URI（体重計/ハートなどテーマに合う絵柄）
- **footer**: AGENTS.md 指定の固定マークアップを使用（リンク先・テキストは変更不可、色はテーマに合わせ調整可）

### 主要関数（インライン `<script>`）

- `calcBmi(heightCm, weightKg)` — BMI 生値を返す純関数。`weightKg / (heightCm / 100) ** 2`
- `classifyBmi(bmi)` — BMI 値から判定ラベル文字列を返す純関数。境界は下限含む・上限含まず（例: 25.0 は「肥満（1度）」）
- `render()` — 両入力値を読み取り、妥当（数値かつ > 0）なら BMI を `toFixed(1)` で表示して判定ラベルを更新、不当ならプレースホルダ表示に戻す
- 両 `<input>` の `input` イベントに `render` を登録し、変更のたびに再計算する

### テスト

`tests/app.spec.ts` に受け入れ条件それぞれに対応する Playwright テストを追記する（既存スモークテストは残す）。`page.fill()` で入力し、結果表示領域のテキストをアサートする。境界値（18.5、25.0）の区分判定もテストに含める。

### 完成条件

- PLAN.md の受け入れ条件をすべて満たす
- `npm run verify` と `npm test` が通る
